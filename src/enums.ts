export enum Locales {
  en = "en",
  es = "es"
}

export enum CustomHeaders {
  Locale = "x-selected-locale",
  Pathname = "x-request-pathname",
  HasMultipleLocales = "x-has-multiple-locales",
}

export enum BooleanString {
  yes = "YES",
  no = "FALSE"
}